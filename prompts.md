Prompt 1: Convert to Cypress POM
Convert the following TestRigor test case into Cypress using the Page Object Model (POM).
Use best practices, avoid brittle selectors, and prefer text-based or accessible selectors.

Prompt 2: Improve selectors
Improve the Cypress selectors to make them more stable for a React application. Avoid using fragile selectors like class names or indexes.

Promt 3: Add monitor Order

add a POM code in cypress with Java https://github.com/vanesinha405-sudo/Purchase-Laptop.git to order a monito "Apple monitor 24"